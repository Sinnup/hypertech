# HT-547E64 — LangGraph Studio shows all agents connected

- **Scenario**: fix
- **Agents**: (graph topology / visualization)
- **Branch**: feature/HT-547E64
- **Created**: 2026-06-26 03:20 UTC
- **Status**: completed

## Summary
In LangGraph Studio only orchestrator, validation_gate, context_packer and
human_escalation appeared connected; the specialist agents looked isolated.
Cause: `add_conditional_edges("context_packer", _plan_router, {})` passed an
empty path map, so LangGraph had no declared destinations to draw from
context_packer to the agents.

## Changes
- **core/graph/workflow.py**: build the full destination map for the
  context_packer conditional edge (every visible agent + END). Pure
  visualization fix — `_plan_router` still returns the real next agent at
  runtime, so execution is unchanged.

## Verification
- `build().get_graph()` now shows `context_packer → {all 14 agents, END}`.
- `uv run pytest tests/e2e/` → 30 passed (context_packer still does not route to
  validation_gate; routing/loop regression tests still green).
