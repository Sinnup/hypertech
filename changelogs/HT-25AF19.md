# HT-25AF19 — LangSmith Dual-Tracing + Colima Docker Runtime

- **Scenario**: feature
- **Agents**: core/tracing, core/events, infra (docker)
- **Branch**: feature/HT-25AF19
- **Created**: 2026-06-24 16:09 UTC
- **Status**: in_progress

## Summary

Add LangSmith tracing alongside existing Langfuse to get LangGraph-native node/edge topology
visualization. LangSmith's `LangChainTracer` callback captures graph DAG structure, node
transitions, state diffs, and conditional edge routing — all things Langfuse's flat OTEL
span tree cannot show. Both systems coexist without interference because LangSmith hooks
into LangChain's callback system while Langfuse uses OpenTelemetry context propagation.

Additionally, migrate Docker runtime from Docker Desktop to Colima (already installed)
to avoid needing Docker Desktop open. Colima uses macOS Virtualization.Framework and is
already running with Docker context set to `colima`.

## Changes

### LangSmith Dual-Tracing
- **core/tracing/langsmith.py** (new): `ensure()` validates env vars for auto-tracing — LangSmith auto-detects LangGraph, no manual callback needed (per `langsmith-trace` skill `<trace_langchain_oss>` pattern)
- **main.py**: call `ensure_langsmith()` at startup to configure LANGSMITH_* env vars
- **.env**: set `LANGSMITH_TRACING=true`, `LANGSMITH_API_KEY`, `LANGSMITH_PROJECT=hypertech`
- **requirements.txt**: add `langsmith`

### Colima Docker Migration
- **docker-compose.yml**: verify compatibility with Colima's Docker runtime
- **CLAUDE.md**: update infrastructure section to reference Colima instead of Docker Desktop
- **docs/colima-setup.md** (new): Colima installation and usage guide

## Commits
- `1186bb0` feat: add LangSmith dual-tracing for LangGraph node/edge topology
