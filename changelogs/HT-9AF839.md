# HT-9AF839 — Substep instrumentation + /reload-kb visual steps (Part 4)

- **Scenario**: feature
- **Branch**: feature/HT-9AF839
- **Created**: 2026-06-26 05:20 UTC
- **Status**: completed

## Summary
Part 4 (final): wire real substeps everywhere and give /reload-kb a visual
staged progress in /viz.

## Changes
- **core/notifications/slack.py**: `status()` now also emits a viz
  `agent_progress` substep — every existing agent status message becomes a live
  sub-state with zero per-agent edits.
- **ingestion/google_drive/ingest.py**: progress callback is now `(stage, detail)`
  (download/load/embed/upsert/done) with **per-batch upsert** detail — no more
  silent "slow part".
- **core/notifications/slack_commands.py:_handle_reload_kb**: `emit_plan(KB-RELOAD,
  command, …)` + `step_update` per stage (gray → color strip), one Slack line per
  stage change (no per-batch spam).
- **core/server/viz_routes.py**: lobby broadcast forwards `plan_ready` so a
  no-ticket /viz auto-switches to a command (e.g. KB-RELOAD).
- **static/viz.html**: track the active agent so `slack.status` substeps attribute
  correctly.

## Verification
- `uv run pytest tests/e2e/` → 36 passed (staged ingest progress incl. per-batch).
- `node --check` viz JS OK. Ingest→SSE delivers plan_ready/step_update/agent_progress.

## Commits
- (pending)
