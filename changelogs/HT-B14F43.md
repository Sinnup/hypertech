# HT-B14F43 — Plan/progress event framework + report_step + orchestrator plan

- **Scenario**: feature
- **Branch**: feature/HT-B14F43
- **Created**: 2026-06-26 04:45 UTC
- **Status**: completed

## Summary
Part 2 of the progress visualizer: the event framework for expectation-first,
substep-level progress.

## Changes
- **core/events/graph_events.py**: `emit_plan(ticket,kind,label,steps)` →
  `plan_ready`; `emit_progress(ticket,agent,step,detail)` → `agent_progress`.
- **core/events/reporting.py** (new): `report_step(ticket,agent,message)` posts
  to Slack AND emits a viz substep — one call updates both surfaces.
- **agents/orchestrator/agent.py**: after planning, `emit_plan(...)` (the expected
  steps, gray in /viz) and posts a numbered `📋 Plan (N steps): 1.… → N.…` to Slack.

## Verification
`uv run pytest tests/e2e/test_progress_events.py` → 2 passed (event shapes;
report_step does both). Full suite green.

## Commits
- (pending)
